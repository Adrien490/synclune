// ============================================================================
// CONFIGURATION
// ============================================================================

generator client {
  provider      = "prisma-client"
  output        = "../app/generated/prisma"
  compilerBuild = "small" // Optimized bundle for Vercel serverless
}

datasource db {
  provider = "postgresql"
}

// ============================================================================
// AUTH & USERS
// ============================================================================

enum Role {
  USER
  ADMIN
}

// === STATUT DE COMPTE (RGPD) ===
enum AccountStatus {
  ACTIVE
  INACTIVE
  PENDING_DELETION
  ANONYMIZED
}

model User {
  id               String    @id @default(cuid(2))
  role             Role      @default(USER)
  name             String?   @db.VarChar(100)
  email            String    @unique @db.VarChar(255)
  emailVerified    Boolean   @default(false)
  image            String?   @db.VarChar(2048)
  stripeCustomerId String?   @unique @db.VarChar(50)
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt
  deletedAt        DateTime?
  suspendedAt      DateTime? // Date de suspension du compte

  // === STATUT DE COMPTE (RGPD) ===
  accountStatus AccountStatus @default(ACTIVE)

  // === CONSENTEMENT RGPD ===
  termsAcceptedAt   DateTime? // Date d'acceptation CGV + politique de confidentialite
  termsVersion      String?   @db.VarChar(20) // Version des CGV/politique acceptee (SSOT shared/constants/legal-versions.ts)
  marketingOptOutAt DateTime? // Opposition marketing (Art. 21 RGPD) — bloque back-in-stock

  // === ANONYMISATION RGPD ===
  anonymizedAt        DateTime? // Date d'anonymisation effective
  deletionRequestedAt DateTime? // Demande de suppression enregistree

  sessions       Session[]
  accounts       Account[]
  orders         Order[]
  cart           Cart?
  addresses      Address[]
  wishlist       Wishlist?
  discountUsages DiscountUsage[]

  // Relations avec les remboursements (audit)
  refundsCreated Refund[] @relation("RefundCreator")

  // Gardes DB (SQL brut, SSOT `prisma/sql/raw-guards.sql`) :
  //   User_name_unaccent_trgm_idx   GIN (immutable_unaccent(COALESCE(name, '')))
  //   User_email_unaccent_trgm_idx  GIN (immutable_unaccent(email))
  //     Recherche admin floue insensible aux diacritiques (index d'expression).
  //   User_email_lower_key          UNIQUE (lower("email"))
  //   User_email_lowercase          CHECK ("email" = lower("email"))
  //     ⚠️ Le `@unique` ci-dessus ne couvre que l'égalité BINAIRE : `Alice@x.com` et
  //     `alice@x.com` y passaient comme deux comptes. Le coût réel n'était pas le
  //     doublon mais le garde de compte révoqué de `/sign-in/email` (auth.ts), qui
  //     minuscule l'entrée puis compare la colonne en exact — une ligne en casse
  //     mixte le faisait échouer EN SILENCE (compte suspendu qui se reconnecte).
  //     Les trois chemins d'écriture (email/mdp, Google, changeEmail) sont normalisés
  //     par `databaseHooks.user.{create,update}.before` ; ces deux gardes en font un
  //     invariant de base plutôt qu'une discipline d'appelant.

  @@index([accountStatus, deletionRequestedAt]) // process-account-deletions cron
  @@index([role, deletedAt]) // admin count guard + listing role filter
  @@index([deletedAt, suspendedAt]) // admin listing soft-delete + suspension filter
}

model Session {
  id        String   @id
  expiresAt DateTime
  token     String   @db.VarChar(500)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  ipAddress String?  @db.VarChar(45)
  userAgent String?  @db.VarChar(500)
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([token])
  @@index([userId, expiresAt]) // Couvre aussi userId seul
}

model Account {
  id                    String    @id
  accountId             String
  providerId            String
  userId                String
  user                  User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  accessToken           String?   @db.Text
  refreshToken          String?   @db.Text
  idToken               String?   @db.Text
  accessTokenExpiresAt  DateTime?
  refreshTokenExpiresAt DateTime?
  scope                 String?
  password              String?   @db.VarChar(255)
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt

  @@index([userId])
  @@index([providerId, accountId])
}

model Verification {
  id         String   @id
  identifier String
  value      String   @db.VarChar(500)
  expiresAt  DateTime
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@index([identifier])
}

model Address {
  id     String @id @default(cuid(2))
  userId String
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  firstName  String  @db.VarChar(50)
  lastName   String  @db.VarChar(50)
  address1   String  @db.VarChar(255)
  address2   String? @db.VarChar(255)
  postalCode String  @db.VarChar(10)
  city       String  @db.VarChar(100)
  country    String  @default("FR") @db.VarChar(2)
  phone      String  @db.VarChar(20)

  isDefault Boolean @default(false)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  // Note: Contrainte unique partielle ajoutee via migration SQL brute
  // pour garantir qu'un seul enregistrement a isDefault: true par userId

  // Gardes DB (SQL brut, SSOT `prisma/sql/raw-guards.sql`) :
  //   Address_userId_isDefault_unique  UNIQUE ("userId") WHERE "isDefault" = true
  //     Une seule adresse par defaut par client (index unique PARTIEL — non
  //     exprimable en Prisma).

  @@index([userId, isDefault]) // Couvre aussi userId seul
}

// ============================================================================
// CATALOGUE - ENUMS
// ============================================================================

enum ProductStatus {
  DRAFT
  PUBLIC
  ARCHIVED
}

enum CollectionStatus {
  DRAFT
  PUBLIC
  ARCHIVED
}

enum MediaType {
  IMAGE
  VIDEO
}

// ============================================================================
// CATALOGUE - ENUMS ADDITIONNELS
// ============================================================================

/// Source d'une action sur une commande (audit trail)
enum HistorySource {
  ADMIN
  WEBHOOK
  SYSTEM
  CUSTOMER
}

// ============================================================================
// CATALOGUE - REFERENTIELS
// ============================================================================

model ProductType {
  id          String  @id @default(cuid(2))
  slug        String  @unique @db.VarChar(100)
  label       String  @unique @db.VarChar(50)
  description String? @db.Text
  isActive    Boolean @default(true)
  isSystem    Boolean @default(false) // Types systeme non modifiables/supprimables

  products Product[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([isActive])
}

model Color {
  id          String            @id @default(cuid(2))
  slug        String            @unique @db.VarChar(100)
  name        String            @unique @db.VarChar(100)
  hex         String            @db.VarChar(7)
  description String?           @db.Text // Notes créatrice (or rose 18K mat, hypoallergénique, etc.)
  isActive    Boolean           @default(true) // Coherence avec Material
  skuColors   ProductSkuColor[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([isActive])
}

model Material {
  id           String               @id @default(cuid(2))
  slug         String               @unique @db.VarChar(100)
  name         String               @unique @db.VarChar(100)
  description  String?              @db.Text
  isActive     Boolean              @default(true)
  skuMaterials ProductSkuMaterial[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Filtre fréquent sur getMaterialOptions (where isActive: true) + listing admin
  @@index([isActive])
}

/// Table de jointure explicite ProductSku <-> Color (many-to-many)
/// Permet à une variante d'être composée de plusieurs couleurs (bague bicolore
/// or rose + argent, dégradé tricolore, etc.).
/// L'ordre (`position`) reflète la priorité : position=0 = couleur principale
/// (utilisée pour vignette listing/PDP, snapshot OrderItem joint dans l'ordre).
model ProductSkuColor {
  id      String     @id @default(cuid(2))
  skuId   String
  sku     ProductSku @relation(fields: [skuId], references: [id], onDelete: Cascade)
  colorId String
  // onDelete: Restrict — préserve la protection anti-orphans (parité matériaux)
  color   Color      @relation(fields: [colorId], references: [id], onDelete: Restrict)

  position Int @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([skuId, colorId])
  @@index([colorId]) // FK reverse lookup (filtre storefront + merge-colors)
  @@index([skuId, position]) // récupération ordonnée des couleurs d'un SKU
}

/// Table de jointure explicite ProductSku <-> Material (many-to-many)
/// Permet à une variante d'être composée de plusieurs matériaux.
/// L'ordre (`position`) reflète la priorité : position=0 = matériau principal
/// (utilisé pour SEO/care-tips, snapshot OrderItem joint dans l'ordre).
model ProductSkuMaterial {
  id         String     @id @default(cuid(2))
  skuId      String
  sku        ProductSku @relation(fields: [skuId], references: [id], onDelete: Cascade)
  materialId String
  // onDelete: Restrict — préserve la protection anti-orphans (audit 2026-05-11 P1.1)
  material   Material   @relation(fields: [materialId], references: [id], onDelete: Restrict)

  position Int @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([skuId, materialId])
  @@index([materialId]) // FK reverse lookup (filtre storefront + merge-materials)
  @@index([skuId, position]) // récupération ordonnée des matériaux d'un SKU
}

// ============================================================================
// CATALOGUE - COLLECTIONS
// ============================================================================

model Collection {
  id          String           @id @default(cuid(2))
  name        String           @unique @db.VarChar(100)
  slug        String           @unique @db.VarChar(100)
  description String?          @db.Text
  status      CollectionStatus @default(DRAFT)

  createdAt DateTime            @default(now())
  updatedAt DateTime            @updatedAt
  products  ProductCollection[]

  // Used by getPublicCollectionSlugs (status = PUBLIC),
  // getCollectionOptions (status IN [DRAFT, PUBLIC]),
  // getCollectionCountsByStatus (groupBy status), and storefront/sitemap filters.
  @@index([status])
}

/// Table de jointure explicite Product <-> Collection (many-to-many)
/// Permet a un produit d'appartenir a plusieurs collections
model ProductCollection {
  id           String     @id @default(cuid(2))
  productId    String
  product      Product    @relation(fields: [productId], references: [id], onDelete: Cascade)
  collectionId String
  collection   Collection @relation(fields: [collectionId], references: [id], onDelete: Cascade)

  /// Date d'ajout a la collection (pour tri automatique par nouveautes)
  addedAt DateTime @default(now())

  /// Produit mis en avant dans la collection (pour hero/banner)
  /// Contrainte DB: un seul produit featured par collection
  /// Index unique partiel via migration 20260417 (WHERE "isFeatured" = true)
  isFeatured Boolean @default(false)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Gardes DB (SQL brut, SSOT `prisma/sql/raw-guards.sql`) :
  //   ProductCollection_collectionId_isFeatured_unique
  //     UNIQUE ("collectionId") WHERE "isFeatured" = true
  //     Un seul produit mis en avant par collection (hero/banner).

  /// Un produit ne peut appartenir qu'une seule fois a une collection
  @@unique([productId, collectionId])
  @@index([collectionId]) // FK reverse lookup
}

// ============================================================================
// CATALOGUE - PRODUITS & SKUs
// ============================================================================

model Product {
  id          String        @id @default(cuid(2))
  slug        String        @unique @db.VarChar(100)
  title       String        @db.VarChar(200)
  description String?       @db.Text
  status      ProductStatus @default(PUBLIC)

  type   ProductType? @relation(fields: [typeId], references: [id], onDelete: SetNull)
  typeId String?

  collections ProductCollection[]

  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  deletedAt DateTime? // Soft delete pour preserver historique commandes

  skus       ProductSku[]
  orderItems OrderItem[]

  // Wishlist
  wishlistItems WishlistItem[]

  // Gardes DB (SQL brut, SSOT `prisma/sql/raw-guards.sql`) :
  //   Product_title_unaccent_trgm_idx        GIN (immutable_unaccent(title))
  //   Product_description_unaccent_trgm_idx  GIN (immutable_unaccent(description))
  //     Recherche admin floue et insensible aux diacritiques (« creole » trouve
  //     « creole »). Index d'EXPRESSION : non exprimables en Prisma.

  @@index([typeId])
  @@index([status, createdAt(sort: Desc)]) // Admin: filtrage catalogue par statut
  @@index([status, deletedAt]) // hard-delete-retention cron (soft-deleted ARCHIVED >= 10y)
}

model ProductSku {
  id        String  @id @default(cuid(2))
  sku       String  @unique @db.VarChar(100)
  productId String
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  // Couleurs multi (M2M via ProductSkuColor). Un SKU peut être composé de
  // plusieurs couleurs (ex: bague bicolore or rose + argent). Ordre = priorité,
  // la 1re (position=0) est la couleur « principale » utilisée pour la vignette
  // listing/PDP et le snapshot OrderItem.
  // Cap applicatif : ARRAY_LIMITS.SKU_COLORS (3).
  colors ProductSkuColor[]

  // Matériaux multi (M2M via ProductSkuMaterial). Un SKU peut être composé de
  // plusieurs matériaux (ex: bague verre + acier). Ordre = priorité, le 1er
  // (position=0) est le matériau « principal » utilisé pour SEO/care-tips.
  // Cap applicatif : ARRAY_LIMITS.SKU_MATERIALS (3).
  materials ProductSkuMaterial[]

  size String? @db.VarChar(50)

  // ATTENTION: Nom legacy trompeur. Il s'agit du PRIX FINAL paye par le client (en centimes).
  // En micro-entreprise (art. 293 B CGI), aucune TVA n'est ajoutee : prix affiche = prix paye.
  // Si bascule au regime reel TVA un jour, ce champ restera le prix final TTC (TVA incluse).
  // Exemple : 30 euros = 3000 centimes
  priceInclTax   Int
  compareAtPrice Int? // Prix avant reduction (optionnel, pour afficher prix barre)
  inventory      Int     @default(0)
  isActive       Boolean @default(true)

  isDefault Boolean @default(false)

  images         SkuMedia[]
  orderItems     OrderItem[]
  cartItems      CartItem[]
  stockMovements StockMovement[]
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt
  deletedAt      DateTime? // Soft delete pour preserver historique commandes

  // Note 2026-06-24 (AUDIT) : il n'existe PLUS d'index unique d'identité de variante
  // (productId × size). L'index partiel "ProductSku_productId_colorId_size_active_key"
  // a été DROPPÉ par la migration M2M 20260515181712_add_sku_colors_m2m sans être
  // recréé (deux SKU même produit/taille avec combos couleurs distincts doivent
  // coexister). L'unicité retombe sur `sku @unique` + la validation applicative à la
  // création — AUCUNE contrainte DB ne garantit l'unicité produit×taille.
  //
  // DÉCISION ASSUMÉE (audit schéma 2026-07-26) : on ne la rétablit PAS en base.
  // La vraie clé est (produit × taille × ENSEMBLE de couleurs) ; les couleurs
  // vivant dans une table de jointure, un index Postgres ne peut pas l'exprimer
  // sans dénormaliser (colonne de signature + trigger de maintenance) — de
  // l'over-engineering à l'échelle de Synclune (une admin, ~20 cmd/mois).
  // L'invariant repose donc sur `assertUniqueVariantCombination`
  // (persist-sku-helpers.service.ts), qui prend un advisory lock transactionnel
  // par produit AVANT de lire les candidats — donc résistant à la concurrence.
  // Appelée par create-sku, update-sku, restore-sku ET update-sku-status
  // (activation : `duplicate-sku` crée sciemment une copie d'identité identique
  // en isActive:false, « Dupliquer puis Activer » la publiait sans contrôle).
  // Verrouillé par test/contract/sku-variant-identity-guard.contract.test.ts
  // + modules/skus/services/__tests__/variant-identity-lock.regression.test.ts.
  //
  // Contrainte DB: un seul SKU par defaut par produit (active SKUs uniquement)
  // Index unique partiel via migration 20260417 (WHERE "isDefault" = true AND "deletedAt" IS NULL)
  //
  // Gardes DB (SQL brut, SSOT `prisma/sql/raw-guards.sql`) :
  //   ProductSku_inventory_non_negative      CHECK (inventory >= 0)
  //   ProductSku_priceInclTax_positive       CHECK ("priceInclTax" > 0)
  //   ProductSku_compareAtPrice_valid        CHECK (compareAtPrice IS NULL OR compareAtPrice >= priceInclTax)
  //     Un prix barré INFÉRIEUR au prix payé est une fausse remise (Omnibus /
  //     art. L112-1-1 C. conso) : la base le refuse, pas seulement le formulaire.
  //   ProductSku_productId_isDefault_unique  UNIQUE ("productId")
  //     WHERE "isDefault" = true AND "deletedAt" IS NULL — un seul SKU par défaut
  //     par produit, index unique PARTIEL (non exprimable en Prisma).

  // Index FK explicite : couvre les lookups par produit y compris SKU soft-deleted.
  // Les index uniques partiels ci-dessous (WHERE "deletedAt" IS NULL) ne couvrent
  // productId en prefixe que pour les lignes actives.
  @@index([productId])
}

enum StockMovementSource {
  MANUAL_ADJUST // Câblé : formulaire admin d'ajustement de stock (adjust-sku-stock)
  SKU_UPDATE // Câblé : delta inventaire saisi via le formulaire d'édition SKU (update-sku)
  ORDER // Câblé : vente (décrément webhook/cron), encaissement manuel, restock d'annulation
  WEBHOOK // Câblé : restauration de stock via webhook Stripe (PI annulé / commande annulée)
  SYSTEM // Câblé : restock de remboursement (process-refund, reconcile-refunds)
}

/// Audit append-only des mouvements de stock SKU.
///
/// Périmètre : TOUS les mouvements d'inventaire, manuels comme automatiques.
///  - `MANUAL_ADJUST` — adjust-sku-stock (admin)
///  - `SKU_UPDATE`    — update-sku, delta relatif (admin)
///  - `ORDER`         — vente (décrément webhook `payment_intent.succeeded` + cron
///                      sync-async-payments), encaissement manuel (mark-as-paid),
///                      restock d'annulation admin (cancel-order)
///  - `WEBHOOK`       — restauration de stock (restoreStockForOrder,
///                      markOrderAsCancelled)
///  - `SYSTEM`        — restock de remboursement (process-refund, reconcile-refunds)
///
/// ⚠️ Élargi le 2026-07-30 (audit « validation stock panier », STOCK-LEDGER-001).
/// Le périmètre précédent était admin-seul, au motif qu'« à ~20 commandes/mois, un
/// ajustement manuel inexpliqué est la seule cause plausible d'un stock incohérent ».
/// Ce raisonnement a été démenti : STOCK-DOUBLE-CREDIT-001 créditait le stock deux
/// fois (cancel-order restockait inline PUIS posait `RefundItem.restock: true`, que
/// processRefund honorait), sans aucun acteur humain et sans laisser de trace. Le
/// `CHECK inventory >= 0` ne borne que le plancher : un sur-comptage est silencieux.
/// D'où la règle : **toute mutation de `ProductSku.inventory` écrit son mouvement
/// dans la MÊME transaction**, et lit `previousInventory` au moment de l'écriture
/// (`UPDATE … RETURNING`, ou la valeur retournée par l'update Prisma) — jamais depuis
/// un SELECT préalable, qu'un writer concurrent peut invalider en READ COMMITTED.
///
/// ⚠️ WRITE-ONLY ASSUMÉ : aucune requête `data/` ne lit cette table et aucune UI ne
/// l'affiche. C'est délibéré — sa valeur est d'exister le jour où un écart d'inventaire
/// devra être expliqué (consultation via Prisma Studio / SQL), pas d'alimenter un écran.
///
/// Immuable : pas de `updatedAt`, pas de `deletedAt` (cf. OrderHistory, Art. L123-22).
/// `createdById` est un String simple (PAS de relation User) + `createdByName`
/// dénormalisé, aligné sur OrderHistory.authorId/authorName : conserve l'audit
/// même si l'admin est supprimé/anonymisé (RGPD).
model StockMovement {
  id String @id @default(cuid(2))

  skuId String
  sku   ProductSku @relation(fields: [skuId], references: [id], onDelete: Cascade)

  /// Dénormalisé pour les requêtes/agrégations par produit sans join SKU
  productId String

  previousInventory Int
  newInventory      Int
  delta             Int // = newInventory - previousInventory (signé)

  reason String? @db.VarChar(500) // aligné sur adjustSkuStockSchema.reason.max(500)

  source StockMovementSource @default(MANUAL_ADJUST)

  createdById   String? // id admin (null = action système future)
  createdByName String? @db.VarChar(100) // dénormalisé audit

  createdAt DateTime @default(now())

  // CHECK constraints (DB, SQL brut — non exprimables en Prisma) :
  //   StockMovement_delta_consistent        CHECK ("newInventory" = "previousInventory" + "delta")
  //   StockMovement_inventory_non_negative  CHECK ("previousInventory" >= 0 AND "newInventory" >= 0)
  //
  // Un seul index : la table n'a aucun lecteur applicatif (cf. docstring). Celui-ci
  // sert la consultation manuelle « historique d'un SKU », le seul accès réaliste.
  // Les index [productId, createdAt] et [createdById] ont été droppés (audit 2026-07-26) :
  // aucun consommateur, présent ni possible sans surface de lecture.
  @@index([skuId, createdAt(sort: Desc)])
}

model SkuMedia {
  id    String     @id @default(cuid(2))
  skuId String
  sku   ProductSku @relation(fields: [skuId], references: [id], onDelete: Cascade)

  url          String    @db.VarChar(2048)
  thumbnailUrl String?   @db.VarChar(2048) // Thumbnail pour videos (poster)
  blurDataUrl  String?   @db.Text // Data URL COMPLETE du placeholder (PNG ThumbHash serveur, JPEG canvas ou WebP ffmpeg pour un poster video) : plusieurs Ko, d'ou @db.Text
  altText      String?   @db.VarChar(255)
  mediaType    MediaType @default(IMAGE)
  isPrimary    Boolean   @default(false)
  position     Int       @default(0) // Position pour ordre d'affichage

  // Dimensions intrinseques de l'image (ou du poster video), lues a l'upload.
  // NULL sur les lignes anterieures a la migration tant que le backfill n'a pas
  // tourne. Permettent un srcSet lightbox avec un `height` reel (0 auparavant).
  width  Int?
  height Int?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Gardes DB (SQL brut, SSOT `prisma/sql/raw-guards.sql`) :
  //   SkuMedia_dimensions_positive  CHECK (width/height NULL ou > 0)
  //   SkuMedia_one_primary_per_sku  UNIQUE ("skuId") WHERE "isPrimary" = true
  //     Un seul media principal par SKU (index unique PARTIEL).

  @@index([skuId, position])
  @@index([skuId, isPrimary])
}

// ============================================================================
// PANIER
// ============================================================================

model Cart {
  id String @id @default(cuid(2))

  userId    String? @unique
  user      User?   @relation(fields: [userId], references: [id], onDelete: Cascade)
  sessionId String? @unique

  expiresAt DateTime?

  // === DISCOUNT CODE APPLIED (pre-checkout) ===
  appliedDiscountCode String? @db.VarChar(30) // Code promo applique au panier
  discountAmountCache Int? // Montant reduction snapshot (centimes, calcule a application)

  items CartItem[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Gardes DB (SQL brut, SSOT `prisma/sql/raw-guards.sql`) :
  //   Cart_owner_required  CHECK ("userId" IS NOT NULL OR "sessionId" IS NOT NULL)
  //     Un panier appartient soit a un compte, soit a une session invite — jamais
  //     a personne (un panier orphelin serait invisible et increvable).

  @@index([expiresAt])
}

model CartItem {
  id     String @id @default(cuid(2))
  cartId String
  cart   Cart   @relation(fields: [cartId], references: [id], onDelete: Cascade)

  skuId String
  sku   ProductSku @relation(fields: [skuId], references: [id], onDelete: Restrict)

  quantity Int @default(1)

  // Prix snapshot au moment de l'ajout au panier (en centimes)
  // Protege contre les changements de prix entre l'ajout et le paiement
  // Exemple: 29,99 euros = 2999 centimes
  priceAtAdd Int

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // CHECK constraints (DB, SQL brut — non exprimables en Prisma) :
  //   CartItem_quantity_positive    CHECK (quantity >= 1)
  //   CartItem_priceAtAdd_positive  CHECK ("priceAtAdd" > 0)

  @@unique([cartId, skuId])
  @@index([skuId])
}

// ============================================================================
// WISHLIST (FAVORIS)
// ============================================================================

model Wishlist {
  id String @id @default(cuid(2))

  // Support utilisateur connecte OU session invite
  userId    String? @unique
  user      User?   @relation(fields: [userId], references: [id], onDelete: Cascade)
  sessionId String? @unique // UUID pour sessions invite (cookie httpOnly)

  items WishlistItem[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Gardes DB (SQL brut, SSOT `prisma/sql/raw-guards.sql`) :
  //   Wishlist_owner_required  CHECK ("userId" IS NOT NULL OR "sessionId" IS NOT NULL)
  //     Symetrie Cart : compte OU session invite, jamais orpheline.
}

model WishlistItem {
  id         String   @id @default(cuid(2))
  wishlistId String
  wishlist   Wishlist @relation(fields: [wishlistId], references: [id], onDelete: Cascade)

  // SetNull permet l'archivage produit sans bloquer les wishlists existantes
  productId String?
  product   Product? @relation(fields: [productId], references: [id], onDelete: SetNull)

  // Back-in-stock notification
  backInStockNotifiedAt DateTime? // Date d'envoi de la notification retour en stock

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Un produit ne peut etre ajoute qu'une seule fois par wishlist
  // Note: Contrainte unique sur champ nullable - validation metier cote applicatif
  @@unique([wishlistId, productId])
  // [productId] seul couvert en prefixe par l'index composite ci-dessous
  @@index([productId, backInStockNotifiedAt]) // For back-in-stock notification queries
}

// ============================================================================
// COMMANDES - ENUMS
// ============================================================================

enum OrderStatus {
  PENDING
  PROCESSING
  SHIPPED
  DELIVERED
  CANCELLED
}

enum PaymentStatus {
  PENDING
  PAID
  FAILED
  // RÉSERVÉ / LEGACY — jamais émis dans le flow Stripe Elements actuel. Vestige du
  // flux Checkout Session retiré (où une session expirée passait EXPIRED). Aujourd'hui
  // l'abandon 3DS / PaymentIntent orphelin écrit FAILED (cf. sync-async-payments.service.ts).
  // Conservé pour rétro-compat données + seed ; pas de drop d'enum Postgres (politique projet).
  EXPIRED

  PARTIALLY_REFUNDED
  REFUNDED
}

enum FulfillmentStatus {
  UNFULFILLED
  PROCESSING
  SHIPPED
  DELIVERED
  RETURNED
}

/// Actions tracables sur les commandes (audit trail)
/// Requis pour conformite legale (Art. L123-22 Code de Commerce)
enum OrderAction {
  CREATED // Creation de la commande
  PAID // Paiement recu
  PROCESSING // Mise en preparation
  SHIPPED // Expedition
  DELIVERED // Livraison confirmee
  CANCELLED // Annulation
  RETURNED // Retour client
  STATUS_REVERTED // Retour a un statut precedent
  TRACKING_UPDATED // Mise a jour du suivi
  ADDRESS_UPDATED // Mise a jour de l'adresse de livraison
  INVOICE_GENERATED // Facture generee
  INVOICE_GENERATION_FAILED // Echec generation/archivage facture (Art. 286 CGI - silent failure audit)
  REFUND_CREATED // Remboursement cree
  REFUND_COMPLETED // Remboursement confirme (Stripe)
  REFUND_FAILED // Echec du remboursement
  DISPUTE_OPENED // Litige ouvert
  DISPUTE_RESOLVED // Litige resolu
  INVOICE_VOIDED // Facture annulee (commande annulee apres facturation)
  INVOICE_ARCHIVED // PDF facture uploade sur UploadThing (snapshot immuable L102 B LPF)
  PDF_ARCHIVE_FAILED // Echec upload UploadThing du PDF facture (audit monitoring 2026-05-28)
  CREDIT_NOTE_FAILED // Echec emission avoir post-refund (intervention admin requise)
  CREDIT_NOTE_GENERATED // Avoir emis (refund partiel ou total) — distinct de INVOICE_VOIDED (full void)
  CREDIT_NOTE_ARCHIVED // PDF avoir uploade sur UploadThing (snapshot immuable L102 B LPF — symetrie INVOICE_ARCHIVED)
  INVOICE_RECONCILED // Anomalie facture rattrapee par cron reconcile-invoices
  INVOICE_DOWNLOADED // Telechargement PDF facture (audit RGPD Art. 32 - EINV-SEC-002)
  BULK_EXPORT // Export CSV de masse (audit RGPD Art. 30 / livre de recettes - EINV-SEC-005)
  // ⚠️ RESERVE / INUTILISE — transmission B2B/B2G sur PDP supprimee au recentrage B2C
  // (2026-05-28). Aucun code n'emet ces valeurs. Conservees car retirer une valeur
  // d'enum Postgres impose de recreer le type (risque > gain). NE PAS les reutiliser
  // pour un autre sens. Cf. docs/INVOICING.md (residus enum PDP_*).
  PDP_SUBMITTED // [reserve] Facture deposee sur la plateforme agreee (PDP/PA)
  PDP_ACCEPTED // [reserve] ACK positif de la plateforme / destinataire
  PDP_REJECTED // [reserve] Rejet de la plateforme
  PDP_RETRY // [reserve] Tentative de retransmission (DLQ cron)
  PDP_ABANDONED // [reserve] Abandon apres retries epuises
  PDP_CANCELLED // [reserve] Annulation par l'emetteur avant ACCEPTED
}

/// Statut de la facture associée à une commande
enum InvoiceStatus {
  PENDING // Facture en attente de génération
  GENERATED // Facture générée (PDF disponible)
  VOIDED // Facture annulée (commande annulée après facturation)
}

/// Régime TVA du vendeur figé au moment de l'émission de la facture.
/// Si Synclune sort de la franchise (depassement du seuil 85 000 EUR/an ventes de biens — cas
/// Synclune ; 37 500 EUR/an pour les prestations de services), les factures anterieures
/// conservent FRANCHISE_BASE et la mention art. 293 B CGI.
enum VatRegime {
  FRANCHISE_BASE // Art. 293 B CGI (regime actuel Synclune — TVA non applicable)
  NORMAL // Regime reel normal
  SIMPLIFIE // Regime reel simplifie
}

/// Methode de paiement client. Typologie alignée sur Stripe `payment_method_details.type`
/// pour conformité e-reporting B2C (arrêté 2022-1299 §4.3, mode de règlement granulaire
/// transmis à la DGFiP). Mapping appliqué par `modules/payments/services/map-stripe-payment-method.ts`.
///
/// - CARD : carte bancaire brute (Visa, MC, CB)
/// - SEPA_DEBIT : prélèvement SEPA
/// - KLARNA : paiement différé Klarna
/// - LINK : Stripe Link (1-click checkout)
/// - WALLET : Apple Pay / Google Pay (wallets superposés à une carte)
/// - BANCONTACT : Bancontact (BE)
/// - OTHER : fallback pour types Stripe non encore mappés
enum PaymentMethod {
  CARD
  SEPA_DEBIT
  KLARNA
  LINK
  WALLET
  BANCONTACT
  OTHER
}

// ============================================================================
// COMMANDES - MODELS
// ============================================================================

model Order {
  id          String  @id @default(cuid(2))
  orderNumber String  @unique @db.VarChar(50)
  userId      String?
  user        User?   @relation(fields: [userId], references: [id], onDelete: SetNull)

  // === IDENTIFIANTS STRIPE (CRITIQUES) ===
  // Flow Elements (PaymentIntents) uniquement — pas de Checkout Session hosted ni
  // de Stripe Invoicing : les colonnes stripeCheckoutSessionId / stripeInvoiceId
  // ont été retirées (jamais peuplées en prod). Cf. migration drop_stripe_cs_invoice.
  stripePaymentIntentId String? @unique // pi_xxx
  stripeCustomerId      String? @db.VarChar(50) // cus_xxx

  // === INFORMATIONS CLIENT ===
  customerEmail String  @db.VarChar(255)
  customerName  String  @db.VarChar(100)
  customerPhone String? @db.VarChar(20)

  // === ADRESSE DE LIVRAISON (DENORMALISEE - SNAPSHOT) ===
  shippingFirstName  String  @db.VarChar(50)
  shippingLastName   String  @db.VarChar(50)
  shippingAddress1   String  @db.VarChar(255)
  shippingAddress2   String? @db.VarChar(255)
  shippingPostalCode String  @db.VarChar(10)
  shippingCity       String  @db.VarChar(100)
  shippingCountry    String  @default("FR") @db.VarChar(2)
  shippingPhone      String  @db.VarChar(20)

  // === MONTANTS (EN CENTIMES) ===
  subtotal       Int
  discountAmount Int    @default(0) // Montant total des reductions appliquees
  // Micro-entreprise: toujours 0 (exoneree TVA - art. 293 B CGI). Champ conserve pour evolutivite
  // si depassement du seuil de franchise (85 000 EUR/an ventes de biens — cas Synclune ;
  // 37 500 EUR/an prestations de services). CHECK >= 0 en DB.
  taxAmount      Int    @default(0)
  shippingCost   Int    @default(0) // Frais de livraison
  total          Int // Formule: MAX(0, subtotal - discountAmount + shippingCost)
  currency       String @default("EUR") @db.VarChar(3)

  // === ECHECS DE PAIEMENT ===
  // Codes d'erreur du DERNIER echec, ecrits par les webhooks payment_intent.payment_failed.
  //
  // ⚠️ WRITE-ONLY ASSUME (audit schema 2026-07-26) : aucune requete applicative ne
  // les selectionne et aucune UI ne les affiche. On les garde DELIBEREMENT — c'est
  // le seul endroit ou retrouver pourquoi une CB a ete refusee quand une cliente
  // appelle, l'historique complet dans WebhookEvent etant purge par la passe de
  // retention. Consultation via Prisma Studio / SQL. NE PAS les supprimer comme
  // "colonnes mortes" : leur valeur est l'apres-coup, pas le runtime.
  paymentFailureCode    String? @db.VarChar(100) // Code Stripe: "card_declined", "expired_card", etc.
  paymentDeclineCode    String? @db.VarChar(100) // Code reseau detaille (Visa/MC)
  paymentFailureMessage String? @db.VarChar(500) // Message lisible pour debug

  // === FACTURATION (Conformité Article 286 CGI) ===
  invoiceNumber      String?        @unique @db.VarChar(30) // Format: F-YYYY-NNNNN (séquentiel par année)
  invoiceStatus      InvoiceStatus? // null = pas encore de facture
  invoiceGeneratedAt DateTime? // Date de première génération de la facture
  invoiceVoidedAt    DateTime? // Horodatage de passage VOIDED (avoir comptable émis - Art. 272-I CGI)

  // === AVOIR / CREDIT NOTE (Art. 272-I CGI - annulation post-facture) ===
  // ⚠️ Unicité PER-TABLE : ce @unique et celui de Refund.creditNoteNumber sont
  // deux contraintes distinctes (Order ∪ Refund). L'unicité GLOBALE du numéro
  // A-YYYY-NNNNN est garantie par (1) l'advisory lock de génération
  // (2_000_000+year, credit-note-sequence.service) qui sérialise la séquence
  // par année + lookup MAX sur l'UNION, et (2) le trigger DB
  // check_credit_note_cross_table_unique (migration 20260709) qui rejette en
  // 23505/P2002 toute écriture d'un numéro déjà présent dans l'autre table —
  // filet contre les écritures SQL manuelles contournant le lock.
  // Double SSOT ASSUMÉ, pas un oubli : décision datée et conditions de réouverture
  // dans @see docs/KNOWN-ISSUES.md (KI-005). Migrer full-void vers
  // Refund.creditNoteNumber toucherait d'un coup la numérotation gap-free, le
  // trigger cross-table et l'archivage PDF des avoirs — le cœur réglementaire — pour
  // un gain de propreté et zéro gain fonctionnel. ⚠️ Ne PAS ajouter d'écriture sur
  // Order.creditNote* depuis un nouveau chemin de code : tout nouvel avoir passe par
  // Refund, sinon la duplication devient une triplication.
  creditNoteNumber      String?   @unique @db.VarChar(30) // Format: A-YYYY-NNNNN (séquentiel par année)
  creditNoteGeneratedAt DateTime? // Date d'émission de l'avoir
  // Archivage PDF avoir (Art. L102 B LPF — symétrie facture). Lazy au 1er download
  // via /api/orders/[orderNumber]/credit-note, idempotent ensuite.
  creditNotePdfUrl      String?   @db.VarChar(2048)
  creditNotePdfHash     String?   @db.VarChar(64)

  // === ARCHIVAGE PDF IMMUABLE (Art. L102 B LPF) ===
  invoicePdfUrl     String?   @db.VarChar(2048) // URL UploadThing du PDF archivé (immuable bit-à-bit)
  invoicePdfHash    String?   @db.VarChar(64) // SHA-256 du PDF (empreinte audit fiscal)
  invoiceArchivedAt DateTime? // Horodatage d'upload UploadThing (cron archive-unarchived cible NULL)

  // === SNAPSHOT INVOICEDATA FIGÉ (Art. L102 B LPF — facture comme données) ===
  // Le PDF n'est qu'un rendu visuel. La "facture" au sens comptable EST le
  // payload InvoiceData figé au moment de l'attribution du numéro. Tout futur
  // changement de template doit pouvoir régénérer un PDF identique à partir
  // de ce snapshot (déterminisme jsPDF garanti par renderInvoicePdf).
  //
  // ⚠️ La FORME de ce payload est versionnée PAR LE PAYLOAD, pas par une colonne :
  // `InvoiceData.formatVersion` (SSOT `INVOICE_DATA_FORMAT_VERSION`,
  // modules/invoices/constants/invoice-data-format.ts). Une colonne serait hors
  // hash et pourrait dériver du contenu qu'elle décrit. Tout changement de forme
  // (ajout/retrait/renommage/changement d'unité) DOIT incrémenter cette constante
  // et traiter l'ancienne version côté lecture — sinon le `as InvoiceData` de
  // `verifyInvoiceSnapshot` rend `undefined` en silence sur les lignes anciennes,
  // dont le hash reste pourtant valide. Snapshot sans `formatVersion` ⇒ version 1.
  // Le snapshot est en outre validé par `invoiceDataSchema` AVANT d'être figé
  // (cohérence somme des lignes == totaux) : un échec diffère la facture via
  // invoiceRetryDeferred au lieu de figer 10 ans un document faux.
  invoiceDataSnapshot Json? // Payload InvoiceData complet (canonical-JSON, clés triées)
  invoiceDataHash     String? @db.VarChar(64) // SHA-256 du snapshot (audit-vérifiable)

  // === SNAPSHOT VENDEUR (Facturation electronique 2026-2027) ===
  //
  // Fige l'identite du vendeur au moment de invoiceGeneratedAt. Si SIRET, raison
  // sociale ou regime TVA changent ulterieurement (demenagement, sortie franchise
  // art. 293 B CGI, evolution forme juridique), les factures historiques restent
  // reconstituables a l'identique (Art. L102 B LPF).
  // Nullable pour compat avec les factures pre-snapshot (regeneration tombe sur
  // getVendorLegalInfo() env en fallback).
  vendorLegalName String?    @db.VarChar(255)
  vendorTradeName String?    @db.VarChar(255)
  vendorAddress   String?    @db.VarChar(500)
  vendorSiren     String?    @db.VarChar(9)
  vendorSiret     String?    @db.VarChar(14)
  vendorVatNumber String?    @db.VarChar(15)
  // Extension snapshot (EINV-FORMAT-007/008 — 2026-05-28). Drift acceptable
  // sur Order pre-migration (fallback env via getVendorLegalInfo()).
  vendorEmail     String?    @db.VarChar(255)
  vendorApeCode   String?    @db.VarChar(10)
  vendorBankIban  String?    @db.VarChar(34)
  vendorBankBic   String?    @db.VarChar(11)
  vendorVatRegime VatRegime?
  vendorLegalForm String?    @db.VarChar(100)

  // === LIVRAISON ===
  // Note 2026-07-26 (AUDIT) : `shippingMethod` (toujours "STANDARD", jamais lu) et
  // `shippingRateId` (vestige des Stripe Shipping Rates, abandonnees avec le flow
  // Checkout Session hosted) ont ete droppes. Le transporteur reel est `shippingCarrier`.
  // Transporteur — SSOT des valeurs : `CARRIERS` (modules/orders/utils/carrier.utils.ts).
  // 11 valeurs : colissimo, lettre_suivie, mondial_relay, chronopost, dpd, gls, dhl,
  // ups, fedex, relais_colis, autre. Aucune contrainte DB (VarChar libre) : la
  // validation est portee par `carrierEnum` (Zod) sur les deux actions ecrivantes.
  shippingCarrier   String?   @db.VarChar(30)
  trackingNumber    String?   @db.VarChar(50)
  trackingUrl       String?   @db.VarChar(2048) // URL de suivi
  estimatedDelivery DateTime? // Date estimee de livraison
  actualDelivery    DateTime? // Date reelle de livraison
  shippedAt         DateTime? // Date d'expedition

  // === STATUTS ===
  status            OrderStatus       @default(PENDING)
  paymentStatus     PaymentStatus     @default(PENDING)
  fulfillmentStatus FulfillmentStatus @default(UNFULFILLED)

  // === PAIEMENT ===
  paymentMethod PaymentMethod @default(CARD)
  paidAt        DateTime?

  // === RELATIONS ===
  items          OrderItem[]
  discountUsages DiscountUsage[]
  refunds        Refund[]
  notes          OrderNote[]
  history        OrderHistory[] // Audit trail (conformite legale)
  disputes       Dispute[]

  // === ADRESSE DE FACTURATION (DENORMALISEE - SNAPSHOT) ===
  billingSameAsShipping Boolean @default(true)
  billingFirstName      String? @db.VarChar(50)
  billingLastName       String? @db.VarChar(50)
  billingAddress1       String? @db.VarChar(255)
  billingAddress2       String? @db.VarChar(255)
  billingPostalCode     String? @db.VarChar(10)
  billingCity           String? @db.VarChar(100)
  billingCountry        String? @db.VarChar(2)
  billingPhone          String? @db.VarChar(20)

  // === DLQ FACTURE (Audit monitoring 2026-05-28 — EINV-OPS-004 / EINV-OPS-008) ===
  // Drapeau pose par ensure-invoice-number / archive-invoice-pdf / void-invoice
  // quand leur operation best-effort echoue. Consomme par cron reconcile-invoices
  // (daily) qui retente, increment invoiceReconcileAttempts, et escalade en
  // alerte admin si > 3. Reset a false apres succes.
  invoiceRetryDeferred     Boolean @default(false)
  invoiceReconcileAttempts Int     @default(0)

  // === SUR-FACTURATION (AM-2 — boucle de réconciliation fermée) ===
  // Posé par le webhook payment_intent.succeeded quand Stripe a encaissé PLUS que
  // Order.total (amount_received > total). On n'auto-rembourse PAS le delta (un
  // refund auto créerait une transaction e-reporting REFUND fantôme divergente de
  // la DGFiP — Invariant 9) : on persiste le trop-perçu (en centimes) et on laisse
  // l'admin rembourser le delta via le flux refund normal. La passe
  // `reconcileOverbilledOrders` du cron `reconcile-refunds` (OVERBILL-RESOLVE-01)
  // auto-résout (pose `overbillingResolvedAt`) dès que les refunds COMPLETED couvrent
  // le delta. Suivi via le dashboard (get-action-items, en PULL).
  overbilledAmountCents Int?
  overbillingResolvedAt DateTime?

  // === PURGE PII À 10 ANS (RGPD Art. 5.1.e — limitation de conservation) ===
  // La commande n'est JAMAIS supprimée physiquement (Art. L123-22), mais la base
  // légale de conservation de la PII (facture portant l'identité — Art. 289 CGI,
  // exemption à l'effacement Art. 17(3)(b) RGPD) expire à paidAt + 10 ans. Au-delà,
  // le cron hard-delete-retention scrubbe les snapshots PII (invoiceDataSnapshot,
  // billing*/customer*/shipping*) et supprime les PDF facture/avoir d'UploadThing,
  // en conservant les données comptables non-PII (numéros, montants, dates).
  // Ce timestamp marque que la purge a eu lieu (évite le retraitement).
  piiPurgedAt DateTime?

  // === INTÉGRITÉ ARCHIVES PDF (Art. L102 B LPF) ===
  // Curseur de rotation du contrôle d'intégrité proactif (cron reconcile-invoices,
  // passe intégrité) : re-hash périodique des PDF archivés (facture + avoir) contre
  // invoicePdfHash/creditNotePdfHash. Posé uniquement quand tous les artefacts
  // présents ont été vérifiés OK (ou réparés) — un échec laisse le curseur en place
  // pour re-priorer l'ordre au prochain run.
  pdfIntegrityCheckedAt DateTime?

  // === SOFT DELETE (Conformite legale - Conservation 10 ans) ===
  // Ne JAMAIS supprimer physiquement une commande (Art. L123-22 Code de Commerce)
  deletedAt DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // CHECK constraints (DB, SQL brut — non exprimables en Prisma) :
  //   Order_subtotal_non_negative           CHECK (subtotal >= 0)
  //   Order_total_non_negative              CHECK (total >= 0)
  //   Order_discountAmount_non_negative     CHECK ("discountAmount" >= 0)
  //   Order_shippingCost_non_negative       CHECK ("shippingCost" >= 0)
  //   Order_taxAmount_non_negative          CHECK ("taxAmount" >= 0)
  //   Order_currency_eur_check              CHECK (currency = 'EUR')
  //   Order_invoiceNumber_format            CHECK ("invoiceNumber" IS NULL OR "invoiceNumber" ~ '^F-[0-9]{4}-[0-9]{5}$')
  //   Order_creditNoteNumber_format_check   CHECK ("creditNoteNumber" IS NULL OR "creditNoteNumber" ~ '^A-[0-9]{4}-[0-9]{5}$')
  //   Order_vendorSiren_format_check        CHECK ("vendorSiren" IS NULL OR "vendorSiren" ~ '^[0-9]{9}$')
  //   Order_vendorSiret_format_check        CHECK ("vendorSiret" IS NULL OR "vendorSiret" ~ '^[0-9]{14}$')
  //   Order_vendorVatNumber_format_check    CHECK ("vendorVatNumber" IS NULL OR "vendorVatNumber" ~ '^[A-Z]{2}[A-Z0-9]{2,13}$')
  //   Order_invoicePdfHash_format_check     CHECK ("invoicePdfHash" IS NULL OR "invoicePdfHash" ~ '^[a-f0-9]{64}$')
  //   Order_creditNotePdfHash_format_check  CHECK ("creditNotePdfHash" IS NULL OR "creditNotePdfHash" ~ '^[a-f0-9]{64}$')
  //   Order_overbilledAmountCents_positive_check CHECK ("overbilledAmountCents" IS NULL OR "overbilledAmountCents" > 0)
  //   Order_invoiceReconcileAttempts_check  CHECK ("invoiceReconcileAttempts" >= 0)
  //   Order_vendorApeCode_format_check      CHECK ("vendorApeCode" ~ '^[0-9]{2}\.[0-9]{2}[A-Z]$')
  //   Order_vendorBankIban_format_check     CHECK ("vendorBankIban" ~ '^[A-Z]{2}[0-9]{2}[A-Z0-9]{4,30}$')
  //   Order_vendorBankBic_format_check      CHECK ("vendorBankBic" ~ '^[A-Z]{6}[A-Z0-9]{2}([A-Z0-9]{3})?$')
  //   Order_total_formula                   CHECK (total = GREATEST(0, subtotal - discountAmount + shippingCost))
  //     ⚠️ L'invariant monétaire le plus fort du schéma : la base REFUSE une
  //     commande dont le total ne dérive pas de ses composantes. Toute évolution
  //     de la formule de prix (frais, taxes, arrondis) doit migrer ce CHECK.
  //   Order_invoiceDataHash_format_check    CHECK ("invoiceDataHash" ~ '^[a-f0-9]{64}$')
  //   Order_invoiceDataSnapshot_hash_coherence_check
  //     CHECK (snapshot et hash tous deux NULL, ou tous deux NON NULL)
  //     Interdit un snapshot de facture sans son empreinte (et l'inverse) : sans
  //     ça, l'immutabilité Art. L102 B LPF ne serait plus vérifiable.
  //
  // Index spéciaux (DB, SQL brut — voir `prisma/sql/raw-guards.sql`) :
  //   Order_customerName_unaccent_trgm_idx / Order_customerEmail_unaccent_trgm_idx
  //     GIN d'expression : recherche admin floue insensible aux diacritiques.
  //     Ce sont les SEULS index d'Order qui vivent en SQL brut — parce qu'ils sont
  //     inexprimables en Prisma, pas parce qu'ils seraient « plus rapides ».
  //
  // ⚠️ Les 4 index de cron déclarés ci-dessous (invoiceRetryDeferred / piiPurgedAt /
  // unpaid_pii_purge / overbilling_unresolved) ont existé en DOUBLE jusqu'au
  // 2026-07-30 : complets ici, puis droppés et recréés en version PARTIELLE
  // (`WHERE …`) par l'annexe des gardes bruts. La base finissait donc dans un état
  // que ce fichier ne décrivait pas — et la prochaine migration générée par
  // `prisma migrate diff` aurait restauré la version complète en silence, effaçant
  // le prédicat. Les partiels ont été retirés de la SSOT (migration
  // 20260730130000_converge_order_shadowed_indexes) : à ≤ 2400 lignes Order sur
  // 10 ans, l'écart partiel / complet / seq scan n'est pas mesurable, alors que la
  // double définition était un piège réel. Les index partiels UNIQUES des autres
  // modèles restent en SQL brut : ce sont des contraintes d'intégrité, pas des
  // optimisations.
  //
  // Triggers (DB, SQL brut — non exprimables en Prisma) :
  //   Order_creditNoteNumber_cross_unique   BEFORE INSERT/UPDATE OF creditNoteNumber — rejette (23505) un numéro déjà présent dans Refund (unicité cross-table séquence A-YYYY)

  // ⚠️ AUDIT 2026-06-24 — à ~20 cmd/mois (≤2400 lignes/10 ans) les index de tri/filtre
  // admin sont DÉCORATIFS (un seq scan + tri en mémoire est instantané). Ne comptent
  // vraiment que : les @unique (intégrité), les GIN trgm SQL (recherche admin), et les
  // index PARTIELS DLQ/purge (crons sur prédicat sélectif). NE PAS rajouter d'index
  // « par anticipation perf » : le repo a déjà dû dégraisser 2× (20260209/20260217).
  @@index([userId, createdAt(sort: Desc)])
  @@index([userId, status, createdAt(sort: Desc)]) // Espace client : "mes commandes" filtrées par statut
  @@index([customerEmail])
  @@index([stripeCustomerId])
  @@index([paymentStatus, createdAt]) // sync-async-payments cron
  @@index([paidAt(sort: Desc)]) // Dashboard KPIs, revenue chart, recent orders, CSV export
  @@index([status, createdAt(sort: Desc)]) // Admin: filtrage commandes par statut
  @@index([paymentStatus, deletedAt, paidAt(sort: Desc)]) // Dashboard: recent paid orders
  @@index([invoiceStatus, invoiceArchivedAt], map: "Order_invoiceStatus_invoicePdfUrl_idx") // cron archive-unarchived (filtre GENERATED + url NULL)
  @@index([invoiceRetryDeferred, paidAt], map: "Order_invoiceRetryDeferred_idx") // Cron reconcile-invoices : index partiel SQL (WHERE invoiceRetryDeferred = true)
  @@index([piiPurgedAt, paidAt], map: "Order_piiPurgedAt_paidAt_idx") // Cron hard-delete-retention : purge PII à paidAt+10 ans (WHERE piiPurgedAt IS NULL)
  @@index([piiPurgedAt, paidAt, createdAt], map: "Order_unpaid_pii_purge_idx") // Cron hard-delete-retention : purge PII commandes jamais payées (WHERE piiPurgedAt IS NULL AND paidAt IS NULL, range createdAt) — RGPD-AUDIT F-A
  @@index([overbillingResolvedAt, overbilledAmountCents], map: "Order_overbilling_unresolved_idx") // dashboard get-action-items + passe reconcileOverbilledOrders (WHERE overbilledAmountCents IS NOT NULL AND overbillingResolvedAt IS NULL)
}

/// Historique des actions sur les commandes (audit trail)
/// Obligatoire pour conformite legale francaise (Art. L123-22 Code de Commerce)
/// Conservation 10 ans avec la commande
/// onDelete: SetNull pour preserver l'audit trail meme si commande supprimee
model OrderHistory {
  id      String  @id @default(cuid(2))
  orderId String?
  order   Order?  @relation(fields: [orderId], references: [id], onDelete: SetNull)

  /// Action effectuee sur la commande
  action OrderAction

  /// Statuts avant/apres pour les changements de statut
  previousStatus            OrderStatus?
  newStatus                 OrderStatus?
  previousPaymentStatus     PaymentStatus?
  newPaymentStatus          PaymentStatus?
  previousFulfillmentStatus FulfillmentStatus?
  newFulfillmentStatus      FulfillmentStatus?

  /// Note explicative (ex: raison annulation, commentaire admin)
  note String? @db.Text

  /// Metadonnees supplementaires (JSON flexible pour donnees additionnelles)
  metadata Json?

  /// Auteur de l'action
  authorId   String? // ID de l'admin ou null pour actions systeme
  authorName String? @db.VarChar(100) // Denormalise pour audit (si admin supprime plus tard)

  /// Source de l'action
  source HistorySource @default(ADMIN)

  /// Timestamp de l'action
  createdAt DateTime @default(now())

  // === INDEXES ===
  @@index([orderId, createdAt(sort: Desc)])
  @@index([authorId])
}

model OrderItem {
  id      String @id @default(cuid(2))
  orderId String
  order   Order  @relation(fields: [orderId], references: [id], onDelete: Cascade)

  // onDelete: SetNull - Preserve l'historique des ventes, le snapshot (productTitle, etc.) reste
  // Un produit soft-deleted rend productId null sans bloquer ni cascader
  productId String?
  product   Product? @relation(fields: [productId], references: [id], onDelete: SetNull)

  // onDelete: Restrict intentionnel - Un SKU avec commandes ne peut pas etre supprime
  // Garantit l'integrite comptable (Art. L123-22 Code de commerce - conservation 10 ans)
  // Pour "supprimer" un SKU : utiliser le soft-delete sur Product ou archiver le SKU
  skuId String
  sku   ProductSku @relation(fields: [skuId], references: [id], onDelete: Restrict)

  // Snapshot des donnees produit au moment de la commande
  productTitle       String  @db.VarChar(200)
  productDescription String? @db.Text
  productImageUrl    String? @db.VarChar(2048)

  skuSku        String? @db.VarChar(100) // Snapshot du code SKU au moment de la commande
  skuColor      String? @db.VarChar(100)
  /// Snapshot des hex codes (CSV "#B76E79,#C0C0C0", ordre = position) pour rendu pastille email/facture.
  skuColorHexes String? @db.VarChar(200)
  skuMaterial   String? @db.VarChar(100)
  skuSize       String? @db.VarChar(50)
  skuImageUrl   String? @db.VarChar(2048)

  // Prix et quantite
  // Micro-entreprise franchise TVA (Art. 293 B CGI) : TVA toujours nulle, le
  // total ligne se derive de price * quantity. Pas de snapshot TVA par ligne.
  price    Int // Prix unitaire FINAL sans TVA en centimes
  quantity Int

  // Relations
  refundItems RefundItem[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // CHECK constraints (DB, SQL brut — non exprimables en Prisma) :
  //   OrderItem_price_positive     CHECK (price > 0)
  //   OrderItem_quantity_positive  CHECK (quantity > 0)

  @@index([orderId])
  @@index([skuId])
  @@index([productId])
}

/// Notes internes sur les commandes (visibles uniquement par les admins)
/// Permet de documenter les echanges clients, incidents, ou actions speciales
model OrderNote {
  id String @id @default(cuid(2))

  orderId String?
  order   Order?  @relation(fields: [orderId], references: [id], onDelete: SetNull)

  // Contenu de la note
  content String @db.Text

  // Admin qui a cree la note
  authorId   String
  authorName String @db.VarChar(100) // Denormalise pour historique si admin supprime

  // Drapeau interne : true = visible uniquement par les admins (fraude suspectee,
  // blacklist, escalade legale, etc.). false = visible aussi cote client si jamais
  // /account/orders expose les notes. Default false pour preserver le comportement
  // historique des notes existantes.
  isInternal Boolean @default(false)

  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  deletedAt DateTime? // Soft delete pour audit trail

  @@index([orderId, createdAt(sort: Desc)])
}

// ============================================================================
// REMBOURSEMENTS
// ============================================================================

enum RefundReason {
  CUSTOMER_REQUEST // Droit de retractation (14 jours)
  DEFECTIVE // Bijou casse / abime
  WRONG_ITEM // Erreur de preparation de commande
  LOST_IN_TRANSIT // Colis perdu par le transporteur
  FRAUD // Fraude averee
  OTHER // Autre
}

enum RefundStatus {
  PENDING // Demande recue / En attente de validation
  APPROVED // Valide, en attente de traitement Stripe
  COMPLETED // Rembourse effectivement (Stripe a confirme)
  REJECTED // Refuse (ex: retour hors delai)
  FAILED // Echec technique Stripe
  CANCELLED // Annule par l'admin (soft delete)
}

model Refund {
  id String @id @default(cuid(2))

  // Relation avec la commande (Restrict pour protection comptable)
  orderId String
  order   Order  @relation(fields: [orderId], references: [id], onDelete: Restrict)

  // Identifiant Stripe du remboursement (pour lier la compta)
  stripeRefundId String? @unique // re_xxxx

  // Montants
  amount   Int // Montant TOTAL rembourse en centimes
  currency String @default("EUR") @db.VarChar(3)

  // Raison et Statut
  reason        RefundReason
  status        RefundStatus @default(PENDING)
  failureReason String?      @db.VarChar(500) // Detail si FAILED (ex: "insufficient_funds", "charge_already_refunded")

  // Note interne (ex: "Le fermoir etait casse, photos recues par mail")
  note String? @db.Text

  // Details des articles rembourses
  items RefundItem[]

  // Audit
  createdBy     String? // ID de l'admin qui a cree le remboursement
  createdByUser User?     @relation("RefundCreator", fields: [createdBy], references: [id], onDelete: SetNull)
  processedAt   DateTime? // Date de traitement effectif (Stripe)

  // Nombre de tentatives Stripe (P0.2 idempotency key rotation).
  // Incremente a chaque `retryFailedRefund` pour generer une nouvelle cle
  // d'idempotence Stripe ; sinon Stripe rejoue la reponse mise en cache
  // (24h pour les erreurs 4xx). Inclus dans `idempotencyKey: refund_${id}_${attemptCount}`.
  attemptCount Int @default(0)

  // ORD-STRIPE-005 : flag DB pour dédup cross-instance de l'email confirmation
  // client. SAGA admin / webhook charge.refunded / cron reconcile-refunds peuvent
  // tous tenter d'envoyer le même email. Resend `idempotencyKey` (24h) seule ne
  // suffit pas si les 3 chemins génèrent des clés différentes (incident).
  // Posé atomiquement avant envoi → si Resend échoue, retry possible via cron.
  confirmationEmailSentAt DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // === AVOIR / CREDIT NOTE (Art. 272-I CGI - Phase 2A) ===
  //
  // Stocke le numero d'avoir par REMBOURSEMENT (vs sur Order auparavant).
  // Permet d'emettre un avoir partiel distinct pour chaque refund partiel
  // COMPLETED, sans confondre avec l'annulation totale de la facture.
  //
  // Cycle de deprecation : les champs equivalents sur Order restent
  // populates par voidInvoice() (full void path) tant que le code n'a pas
  // migre. Le backfill SQL initial copie Order.creditNoteNumber vers le
  // dernier Refund COMPLETED de la commande (idempotent).
  creditNoteNumber      String?   @unique @db.VarChar(30) // Format: A-YYYY-NNNNN
  creditNoteGeneratedAt DateTime?
  creditNotePdfUrl      String?   @db.VarChar(2048)
  creditNotePdfHash     String?   @db.VarChar(64)

  // Curseur de rotation du contrôle d'intégrité proactif des PDF avoir archivés
  // (cron reconcile-invoices, passe intégrité) — symétrie Order.pdfIntegrityCheckedAt.
  pdfIntegrityCheckedAt DateTime?

  // === SOFT DELETE (Conformite legale - Conservation 10 ans) ===
  // Ne JAMAIS supprimer physiquement un remboursement (Art. L123-22 Code de Commerce)
  deletedAt DateTime?

  // CHECK constraints (DB, SQL brut — non exprimables en Prisma) :
  //   Refund_amount_positive    CHECK (amount > 0)
  //   Refund_currency_eur_check CHECK (currency = 'EUR')
  //   Refund_creditNoteNumber_format_check  CHECK ("creditNoteNumber" IS NULL OR "creditNoteNumber" ~ '^A-[0-9]{4}-[0-9]{5}$')
  //   Refund_creditNotePdfHash_format_check CHECK ("creditNotePdfHash" IS NULL OR "creditNotePdfHash" ~ '^[a-f0-9]{64}$')
  //
  // Triggers (DB, SQL brut — non exprimables en Prisma) :
  //   Refund_creditNoteNumber_cross_unique  BEFORE INSERT/UPDATE OF creditNoteNumber — rejette (23505) un numéro déjà présent dans Order (unicité cross-table séquence A-YYYY)

  @@index([orderId])
  @@index([status, processedAt]) // reconcile-refunds cron
  @@index([status, stripeRefundId]) // reconcile-refunds cron (APPROVED + non-null stripeRefundId)
  @@index([createdAt(sort: Desc)]) // Admin: tri chronologique remboursements
}

model RefundItem {
  id String @id @default(cuid(2))

  refundId String
  refund   Refund @relation(fields: [refundId], references: [id], onDelete: Restrict)

  // Lien precis vers la ligne de commande d'origine
  // Cela permet de savoir quel produit exact (et a quel prix d'origine) est rembourse
  orderItemId String
  orderItem   OrderItem @relation(fields: [orderItemId], references: [id], onDelete: Restrict)

  quantity Int // Nombre d'unites remboursees (ex: 1 sur 2 achetees)

  amount Int // Montant rembourse pour CET article (en centimes)
  // Permet de rembourser moins que le prix d'achat si decote appliquee

  // Gestion de stock
  restock Boolean @default(true) // Faut-il remettre le produit en stock (inventory +1) ?
  // false si le produit est abime/casse

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // CHECK constraints (DB, SQL brut — non exprimables en Prisma) :
  //   RefundItem_quantity_positive  CHECK (quantity >= 1)
  //   RefundItem_amount_positive    CHECK (amount > 0)

  @@index([refundId])
  @@index([orderItemId])
}

// ============================================================================
// LITIGES STRIPE (CHARGEBACKS)
// ============================================================================

enum DisputeStatus {
  NEEDS_RESPONSE // Requires evidence submission
  UNDER_REVIEW // Evidence submitted, Stripe reviewing
  WON // Resolved in merchant's favor
  LOST // Resolved in customer's favor
  CHARGE_REFUNDED // Charge was refunded before dispute resolved
}

enum DisputeReason {
  DUPLICATE
  FRAUDULENT
  SUBSCRIPTION_CANCELED
  PRODUCT_UNACCEPTABLE
  PRODUCT_NOT_RECEIVED
  UNRECOGNIZED
  CREDIT_NOT_PROCESSED
  GENERAL
}

/// Litige Stripe (chargeback) associe a une commande
/// Permet de tracker les disputes de maniere structuree au lieu de notes texte libres
model Dispute {
  id              String @id @default(cuid(2))
  stripeDisputeId String @unique // dp_xxx

  // Relation avec la commande (Restrict pour protection comptable)
  orderId String
  order   Order  @relation(fields: [orderId], references: [id], onDelete: Restrict)

  // Montants
  amount   Int // Montant conteste en centimes
  fee      Int    @default(0) // Frais de litige Stripe (~1500 = 15 EUR)
  currency String @default("EUR") @db.VarChar(3)

  // Raison et statut
  reason DisputeReason
  status DisputeStatus @default(NEEDS_RESPONSE)

  // Deadline Stripe pour soumettre les preuves
  dueBy DateTime?

  // Resolution
  resolvedAt DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // CHECK constraints (DB, SQL brut — non exprimables en Prisma) :
  //   Dispute_amount_positive     CHECK (amount > 0)
  //   Dispute_fee_non_negative    CHECK (fee >= 0)
  //   Dispute_currency_eur_check  CHECK (currency = 'EUR')

  @@index([orderId])
}

// ============================================================================
// CODES PROMO & REDUCTIONS
// ============================================================================

enum DiscountType {
  PERCENTAGE // Ex: -20%
  FIXED_AMOUNT // Ex: -10 euros
}

model Discount {
  id   String @id @default(cuid(2))
  code String @unique @db.VarChar(30) // Ex: "BIENVENUE10", "ETE2025"

  // Type et valeur de la reduction
  type  DiscountType
  value Int // En pourcentage (20 = 20%) ou centimes (1000 = 10 euros)

  // Conditions d'utilisation
  minOrderAmount  Int? // Montant minimum de commande en centimes (ex: 5000 = 50 euros)
  maxUsageCount   Int? // Nombre max d'utilisations total (null = illimite)
  maxUsagePerUser Int? // Nombre max d'utilisations par utilisateur (null = illimite)

  // Periode de validite
  startsAt DateTime  @default(now())
  endsAt   DateTime?

  // Statistiques
  usageCount Int @default(0) // Compteur d'utilisations

  // Etat
  isActive Boolean @default(true)

  // Relations
  usages DiscountUsage[]

  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  deletedAt DateTime?

  // CHECK constraints (DB, SQL brut — non exprimables en Prisma) :
  //   Discount_value_positive          CHECK (value > 0)
  //   Discount_percentage_max_100      CHECK (type != 'PERCENTAGE' OR value <= 100)
  //   Discount_usageCount_within_limit CHECK (maxUsageCount IS NULL OR usageCount <= maxUsageCount)
  //   Discount_usageCount_non_negative CHECK (usageCount >= 0) — [[DISC-USAGE-002]]
  //     borne basse : un décrément non gardé (release d'annulation) rendait le
  //     code redeemable au-delà de maxUsageCount. Libération toujours via
  //     `releaseOrderDiscountUsageTx`.
  //   Discount_minOrderAmount_positive  CHECK (minOrderAmount IS NULL OR minOrderAmount > 0)
  //   Discount_maxUsagePerUser_positive CHECK (maxUsagePerUser IS NULL OR maxUsagePerUser > 0)
  //     NULL = pas de condition ; 0 serait un code inutilisable créé par erreur.
}

model DiscountUsage {
  id         String   @id @default(cuid(2))
  discountId String
  discount   Discount @relation(fields: [discountId], references: [id], onDelete: Restrict)

  // Utilisateur (optionnel pour guest checkout)
  userId String?
  user   User?   @relation(fields: [userId], references: [id], onDelete: SetNull)

  // Commande associee (Restrict pour protection comptable)
  orderId String
  order   Order  @relation(fields: [orderId], references: [id], onDelete: Restrict)

  // Snapshot du code promo utilise (le code peut etre modifie apres usage)
  discountCode String @db.VarChar(30)

  // Montant de la reduction appliquee (snapshot)
  amountApplied Int // En centimes

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([discountId, orderId])
  @@index([orderId])
  @@index([discountId, userId]) // Pour verifier usage par utilisateur, couvre aussi discountId seul
  @@index([userId]) // Lookup usage par utilisateur (verification limite promo/user)
}

// ============================================================================
// WEBHOOKS IDEMPOTENCY (Stripe)
// ============================================================================

enum WebhookEventStatus {
  PENDING // Recu, en attente de traitement
  PROCESSING // En cours de traitement
  COMPLETED // Traite avec succes
  FAILED // Echec du traitement
  SKIPPED // Ignore (type non gere ou doublon)
}

/// Evenements webhook Stripe
/// Permet l'idempotence et l'audit trail des webhooks
model WebhookEvent {
  id            String             @id @default(cuid(2))
  stripeEventId String             @unique // evt_xxx - Cle d'idempotence
  eventType     String             @db.VarChar(100) // payment_intent.succeeded, charge.refunded, etc.
  status        WebhookEventStatus @default(PENDING)
  errorMessage  String?            @db.Text
  attempts      Int                @default(0)
  receivedAt    DateTime           @default(now())
  processedAt   DateTime?

  // WEBHOOK-AUDIT-002 : horodatage du DÉBUT du traitement courant, (re)posé à
  // chaque passage en PROCESSING (création/reprise route, claim cron retry-webhooks).
  // Distinct de `receivedAt` (1ère réception, jamais rafraîchie) : sert à mesurer
  // la fraîcheur d'un PROCESSING pour la détection « périmé » (lambda crashée) sans
  // qu'un PROCESSING fraîchement repris par le cron soit pris à tort pour périmé par
  // une redélivrance Stripe concurrente. NULL pour les lignes écrites avant migration
  // (fallback sur receivedAt côté lecture).
  processingStartedAt DateTime?

  // ORD-STRIPE-003 : tâches post-webhook persistées (emails + cache invalidation)
  // pour survivre à un crash de la lambda Vercel après le 200 retourné à Stripe.
  postTasks PostWebhookTask[]

  @@index([status, processedAt]) // rétention + retry queries filter by status then processedAt
  @@index([status, receivedAt]) // rétention par receivedAt + fallback candidats retry (processedAt NULL)
  @@index([status, attempts, processedAt]) // retry-webhooks cron
}

// ============================================================================
// POST-WEBHOOK TASKS (ORD-STRIPE-003 — retry persistant)
// ============================================================================

enum PostWebhookTaskStatus {
  PENDING // Persistée, pas encore exécutée (ou en attente de retry)
  COMPLETED // Exécutée avec succès
  FAILED // Échec définitif après MAX_POST_WEBHOOK_RETRY_ATTEMPTS
}

/// Tâches déclenchées par un webhook (emails transactionnels, invalidation de cache)
/// qui doivent survivre à un crash de la lambda Vercel après le 200 retourné à Stripe.
///
/// Stripe ne re-livre PAS l'événement une fois 200 retourné. Sans persistance, un
/// crash entre `after(...)` start et email-send = email perdu sans backstop.
/// Le cron `retry-post-webhook-tasks` (toutes les 30min) repop PENDING + FAILED
/// dont `attempts < MAX_POST_WEBHOOK_RETRY_ATTEMPTS`.
model PostWebhookTask {
  id String @id @default(cuid(2))

  // Lien vers WebhookEvent (audit trail). SetNull pour préserver la task si
  // l'événement est purgé par la passe de rétention de `cleanup-pending-orders`.
  // Nullable aussi par construction : `sync-async-payments` rejoue les post-tasks
  // d'un webhook définitivement perdu (WEBHOOK-AUDIT-003) sans événement à rattacher.
  webhookEventId String?
  webhookEvent   WebhookEvent? @relation(fields: [webhookEventId], references: [id], onDelete: SetNull)

  /// Type de tâche : "ORDER_CONFIRMATION_EMAIL", "INVALIDATE_CACHE", etc.
  /// Matche `PostWebhookTask["type"]` côté types TypeScript.
  taskType String @db.VarChar(60)

  /// Payload JSON sérialisé : soit `data` (email), soit `{ tags: string[] }` (cache).
  payload Json

  status       PostWebhookTaskStatus @default(PENDING)
  attempts     Int                   @default(0)
  errorMessage String?               @db.Text

  /// Clé d'idempotence applicative (`refund-confirm-XYZ`, `order-confirm-XYZ`).
  /// Sert à dédupliquer côté DB en plus de Resend (24h). Optionnelle pour
  /// les INVALIDATE_CACHE qui n'ont pas besoin de dédup.
  idempotencyKey String? @unique @db.VarChar(255)

  createdAt   DateTime  @default(now())
  processedAt DateTime?

  // WEBHOOK-AUDIT-003 : horodatage de la DERNIÈRE tentative, posé par le claim
  // optimiste de `executeBatch`. Support du backoff par paliers dans
  // `retry-post-webhook-tasks` : sans lui, la sélection ne portait que sur
  // `status + attempts` et les 5 tentatives partaient en ~20 min à cadence fixe.
  // NULL = jamais tentée ⇒ éligible immédiatement (fail-open, lignes legacy).
  lastAttemptAt DateTime?

  // Gardes DB (SQL brut, SSOT `prisma/sql/raw-guards.sql`) :
  //   PostWebhookTask_attempts_non_negative  CHECK ("attempts" >= 0)

  @@index([status, attempts, createdAt]) // cron retry-post-webhook-tasks
  @@index([status, attempts, lastAttemptAt]) // sélection par paliers de backoff
  @@index([status, createdAt]) // passe de rétention (cleanup-pending-orders)
  @@index([webhookEventId]) // audit lookup
}

// ============================================================================
// PARAMETRES BOUTIQUE
// ============================================================================

/// Tonalité visuelle du bandeau d'annonce storefront.
/// PROMO = rose brand (défaut), INFO = bleu neutre, WARNING = ambre (alerte logistique).
enum AnnouncementVariant {
  PROMO
  INFO
  WARNING
}

/// Singleton: parametres globaux de la boutique (1 seule ligne, ID deterministe)
/// Permet de fermer temporairement le storefront (maintenance, vacances, restock)
/// Contrainte DB: CHECK (id = 'store-settings-singleton') via migration 20260417
/// Empeche techniquement toute insertion d'une 2e ligne avec un autre ID
model StoreSettings {
  id             String    @id @default("store-settings-singleton")
  isClosed       Boolean   @default(false)
  closureMessage String?   @db.VarChar(500)
  closedAt       DateTime?
  reopensAt      DateTime?
  closedBy       String?   @db.VarChar(255)

  // === ANNOUNCEMENT BAR (replaces dedicated AnnouncementBar table) ===
  announcementMessage  String?             @db.VarChar(200)
  announcementLink     String?             @db.VarChar(2048)
  announcementStartsAt DateTime?
  announcementEndsAt   DateTime?
  announcementIsActive Boolean             @default(false)
  announcementVariant  AnnouncementVariant @default(PROMO)

  // === CRON STATE : cleanup-orphan-media ===
  // Offset de reprise dans `utapi.listFiles` (audit média M2). Le cron ne balaie
  // que MAX_PAGES_PER_RUN × UPLOADTHING_LIST_LIMIT fichiers par exécution ; sans
  // curseur persistant il repartait de 0 à chaque run et ne voyait JAMAIS au-delà
  // des 2500 premiers fichiers. Or les PDF de facture (1 par commande payée) sont
  // référencés et saturent progressivement cette fenêtre : la collecte d'orphelins
  // devenait un no-op silencieux. Remis à 0 en fin de liste (balayage cyclique).
  orphanMediaScanOffset Int @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Gardes DB (SQL brut, SSOT `prisma/sql/raw-guards.sql`) :
  //   StoreSettings_singleton_id  CHECK (id = 'store-settings-singleton')
  //     C'est CE CHECK qui rend le singleton infranchissable : une 2e ligne avec
  //     un autre id est rejetee par la base, pas seulement par le code.
}
