/**
 * Lien profond « commandes à expédier » : la liste admin filtrée sur PAID —
 * le statut des commandes payées non expédiées. C'est la cible de la pastille
 * de navigation (`badgeUrl`) et de la file d'attente mobile.
 */
export const ORDERS_TO_SHIP_HREF = "/admin/ventes/commandes?filter_status=PAID";
