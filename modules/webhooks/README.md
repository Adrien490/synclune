Module stubbé au lot 2 de la migration lean — réécrit au lot 3 (Stripe Checkout
hébergé : handlers `checkout.session.completed` / `checkout.session.expired`,
idempotence par `Order.stripeSessionId @unique` + gardes de transition).
