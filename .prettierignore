node_modules
.next
out
build
dist
coverage
app/generated
prisma/migrations
playwright-report
test-results
e2e/.auth
e2e/.tmp
public/sw*
public/swe-worker*
pnpm-lock.yaml
*.min.js
*.min.css

# Mirror de la doc Stripe (markdown brut, regénérable : pnpm docs:stripe).
# Sans ces lignes, `pnpm format:check` — donc `pnpm validate` — parcourt
# ~1,6 Mo de markdown qu'on ne maîtrise pas et rougit.
# INDEX.md est écrit à la main et reste formaté normalement.
docs/stripe/*.md
!docs/stripe/INDEX.md

# Brouillons de prompts (Pencil, audits) édités à la main en continu —
# les reformater sous les doigts de leur auteure ferait échouer validate
# à chaque frappe. Non trackés tant qu'ils sont en travail.
docs/prompts

# Artefact généré par l'étape SBOM de la CI (cdxgen) — jamais formaté ni versionné.
sbom.json
