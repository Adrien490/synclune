/** Cookie name for PWA install prompt state */
export const INSTALL_PROMPT_COOKIE_NAME = "install-prompt";

/** Cookie max age: 1 year */
export const INSTALL_PROMPT_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;

/** Minimum visits before showing the banner */
export const INSTALL_PROMPT_MIN_VISITS = 2;

/** Number of dismissals before permanently hiding the banner */
export const INSTALL_PROMPT_MAX_DISMISSALS = 3;
